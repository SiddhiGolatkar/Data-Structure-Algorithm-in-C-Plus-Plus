#include<iostream>
using namespace std;

int main() {

    // Priority for the values inside a brackets () assigned to any variable is
    // given from right to left 

    int a;
    a = (1,2,3) ;

    printf("%d", a) ;

    return 0;

}